import { colorPrimitives, colors } from '../colors'
import { fonts } from '../fonts'
// import space from '../space'

const { red, white, gray } = colorPrimitives

const boxes = {
    primary: colors.primary,
    secondary: red[0],
    muted: gray[7],
    fontFamily: fonts.normal,
    color: white,
    p: 4,
}

export default boxes
